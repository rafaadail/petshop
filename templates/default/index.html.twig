{% extends 'base.html.twig' %}

{% block body %}

    <div class="starter-template">
        <h1 class="display-1">Petshop</h1>
        <p class="lead">Avançando com Symfony 4 - Doctrine</p>
    </div>
    <div class="row">
        <div class="col-md-6">
            <table class="table table-striped">
                {% for qts in qts_animais %}
                    <tr>
                        <th>{{ qts.nome }}</th>
                        <td>{{ qts.qtde }}</td>
                    </tr>
                {% endfor %}
            </table>

        </div>
        <div class="col-md-6">
            <table class="table table-striped">
                {% for qts in qts_racas %}
                    <tr>
                        <th>{{ qts.nome }}</th>
                        <td>{{ qts.qtde }}</td>
                    </tr>
                {% endfor %}
            </table>

        </div>
    </div>

{% endblock %}
